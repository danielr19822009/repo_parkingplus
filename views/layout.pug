doctype html
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        link(rel="stylesheet", href="/css/estilos.css")
        title #{nombrePagina}

        // SweetAlert2 global
        script(src="https://cdn.jsdelivr.net/npm/sweetalert2@11")

    body 
        block header
        block main  
        block footer

        // ⬇⬇⬇ NECESARIO PARA QUE PUG PERMITA SCRIPTS EN CADA VISTA
        block scripts
